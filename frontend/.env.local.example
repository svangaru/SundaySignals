# Supabase — anon key only (read-only, safe to expose in the browser)
NEXT_PUBLIC_SUPABASE_URL=https://qizdcrdzplngqzbricgw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Modal inference endpoint — server-side only (Vercel API route)
# Run `modal deploy pipelines/modal_inference.py` and copy the printed URL
MODAL_INFERENCE_URL=
