# Supabase
SUPABASE_URL=https://qizdcrdzplngqzbricgw.supabase.co
SUPABASE_SERVICE_KEY=        # service role key — used by ingest/training jobs, never expose to frontend
SUPABASE_ANON_KEY=           # anon key — used by Next.js frontend reads only

# Modal (serverless inference)
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
